export interface TypeInfo {
    id: number;
    typeAlphabet: string;
    typeCharacter: string;
    feature: string;
    senseOfValue: string;
}

export interface PoliticianInfo {
    id: number;
    politicianName: string;
    partyName: string;
    explanation: string;
}